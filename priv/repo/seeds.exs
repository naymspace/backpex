# Script for populating the database. You can run it as:
#
#     mix run priv/repo/seeds.exs
#
# Inside the script, you can read and write to any of your
# repositories directly:
#
#     BackpexTestApp.Repo.insert!(%BackpexTestApp.SomeSchema{})
#
# We recommend using the bang functions (`insert!`, `update!`
# and so on) as they will fail if something goes wrong.

# alias BackpexTestApp.Payroll

# for name <- ["Management", "Sales", "Accounting"] do
#   Payroll.create_department(%{name: name})
# end

# for name <- ["Secretary", "Manager"] do
#   Payroll.create_function(%{name: name})
# end
